<section class="hero">
  <div class="overlay"></div>

  <div class="content">
    <h1>Nos hacemos cargo de tu logística en puerto</h1>
    <p>
      Desde el retiro de contenedores hasta la entrega final, operamos tu
      logística reduciendo costos, tiempos y riesgos.
    </p>
    <div class="actions">
      <a href="/#cotizar" class="cta-primary">COTIZAR</a>
      <a href="https://arcentra-portal.vercel.app/" class="cta-secondary"
        >Demo portal</a
      >
    </div>
    <!-- 🔥 NUEVA SECCIÓN TRUST -->
    <div class="trust">
      <div class="trust-item">
        <img
          src="/icon/proteger.png"
          alt="Check"
          width="25"
          height="25"
          color="white"
        />
        <p>Seguridad</p>
      </div>
      <div class="trust-item">
        <img
          src="/icon/reloj.png"
          alt="Check"
          width="25"
          height="25"
          color="white"
        />
        <p>Puntualidad</p>
      </div>
      <div class="trust-item">
        <img
          src="/icon/posicion.png"
          alt="Check"
          width="25"
          height="25"
          color="white"
        />
        <p>Cobertura</p>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    height: 65vh;
    min-height: 650px;
    padding-top: 80px;
    background: url("/hero.png") center/cover no-repeat;
    display: flex;
    align-items: center;
    color: white;
    font-family: "Montserrat", sans-serif;
  }

  /* OVERLAY */
  .overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.85) 0%,
      rgba(0, 0, 0, 0.5) 50%,
      rgba(255, 255, 255, 0.1) 100%
    );
  }

  .content {
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    padding: 0 3rem 3rem 3rem;
    gap: 0.25rem;
  }

  h1 {
    font-size: 2rem;
    font-weight: 600;
    max-width: 700px;
    line-height: 1.2;
    margin-bottom: 20px;
  }

  p {
    font-size: 0.95rem;
    max-width: 500px;
    margin-bottom: 30px;
    color: #e5e5e5;
  }

  .actions {
    display: flex;
    gap: 15px;
    margin-bottom: 30px;
  }

  .cta-primary {
    background: #facc15;
    color: black;
    padding: 12px 22px;
    font-weight: bold;
    font-size: 0.95rem;
    text-decoration: none;
    border-radius: 6px;
    transition:
      transform 0.1s ease,
      box-shadow 0.1s ease;
    width: 10rem;
    text-align: center;
    align-content: center;
  }

  .cta-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(240, 240, 240, 0.3);
  }

  .cta-secondary {
    border: 1px solid rgb(255, 255, 255);
    padding: 12px 22px;
    text-decoration: none;
    color: white;
    border-radius: 6px;
    width: 10rem;
    text-align: center;
    transition:
      background 0.2s ease,
      box-shadow 0.2s ease;
  }

  .cta-secondary:hover {
    font-weight: 700;
    background: rgba(255, 255, 255, 0.5);
  }

  /* 🔥 TRUST MEJORADO */
  .trust {
    display: flex;
    gap: 15px;
    margin-top: 10px;
    flex-wrap: wrap;
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 15px;
    opacity: 0.9;
    color: white;
  }

  .trust-item img {
    filter: invert(100%) sepia(100%) grayscale(100%) brightness(150%);
  }

  .trust-item p {
    margin: 0;
    font-size: 1rem;
    font-weight: 600;
  }

  .trust-item:nth-child(-n + 2)::after {
    content: "|";
    margin-left: 15px;
    opacity: 0.6;
  }

  @media (max-width: 1200px) {
    .content {
      padding-left: 40px;
      padding-right: 40px;
    }

    h1 {
      max-width: 700px;
      font-size: 2rem;
    }
  }

  @media (max-width: 992px) {
    .hero {
      min-height: 550px;
    }

    .content {
      padding: 0 3rem;
    }

    h1 {
      font-size: 1.8rem;
      max-width: 550px;
    }

    p {
      max-width: 450px;
    }

    .actions {
      gap: 10px;
    }

    .cta-primary,
    .cta-secondary {
      width: 9rem;
      font-size: 0.9rem;
    }

    /* 🔥 TRUST MEJORADO */
    .trust {
      gap: 7px;
      margin-top: 5px;
    }

    .trust-item {
      gap: 7px;
    }

    .trust-item p {
      font-size: 0.9rem;
    }

    .trust-item:nth-child(-n + 2)::after {
      margin-left: 7px;
    }
  }

  /* 📱 MOBILE */
  @media (max-width: 768px) {

    .hero {
      padding: 22rem 1rem;
    }

    .content {
      padding: 0 2rem;

    }

    .actions {
      flex-direction: column;
      width: 100%;
    }

    h1 {
      font-size: 1.5rem;
      max-width: 500px;
    }

    p {
      font-size: 0.85rem;
    }

    .cta-primary,
    .cta-secondary {
      width: 160px;
      padding: 10px;
    }

    .trust {
      flex-direction: column;
      gap: 10px;
    }

    .trust-item p {
      font-size: 0.8rem;
    }

    .trust-item:nth-child(-n + 2)::after {
      display: none;
    }
  }

  @media (max-width: 576px) {
    .hero {
      padding: 22rem 1rem;
    }
    
    .content {
      padding: 15px;
    }

    h1 {
      font-size: 1.3rem;
      line-height: 1.3;
    }

    p {
      font-size: 0.8rem;
    }

    .actions {
      width: 100%;
    }

    .cta-primary,
    .cta-secondary {
      width: 100%;
      max-width: 175px;
    }

    .trust-item {
      gap: 12px;
    }

    .trust-item img {
      width: 22px;
      height: 22px;
    }

    .trust-item p {
      font-size: 0.85rem;
    }
  }

  
  @media (max-width: 420px) {
    .hero {
      padding: 22rem 0.25rem;
    }
  }
</style>
